spring:
  main:
    lazy-initialization: true
  application:
    name: bookstoreservice
  # The property server will take default property files
  config:
    import: optional:configserver:http://192.168.70.73:8071
